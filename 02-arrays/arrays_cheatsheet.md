# 📚 Массивы
- **Массив в PHP** — это упорядоченная структура данных, позволяющая хранить несколько значений (элементов) в одной переменной, используя пары «ключ-значение». 
- Это гибкий тип данных, совмещающий функции списка и хеш-таблицы, где ключи могут быть целыми числами или строками.
- В современном PHP используется короткий синтаксис `[]`.

## 1. Типы массивов
- **Индексированные (нумерованные)**: Ключи — целые числа (начинаются с 0).
- **Ассоциативные**: Ключи — строки.
- **Многомерные**: Массивы, содержащие другие массивы.

## 2. Основные функции (Must have)
- `count($arr)` — количество элементов.
- `is_array($arr)` — проверка, является ли переменная массивом.
- `in_array($value, $arr)` — проверяет, есть ли значение в массиве.
- `array_key_exists($key, $arr)` — проверяет наличие ключа.
- `array_merge($arr1, $arr2)` — слияние массивов.
- `array_values($arr)` / `array_keys($arr)` — получение всех значений или ключей.

## 3. Вопросы на интервью (FAQ)
1. **В чем разница между `array_merge` и оператором `+`?**
    - `array_merge` перезаписывает строковые ключи, а `+` оставляет значение от первого массива.
2. **Что будет, если в индексированном массиве удалить элемент через `unset()`?**
    - Индексы **не** пересчитаются автоматически. Появится "дырка". Чтобы пересчитать, нужно использовать `array_values()`.
3. **Как проверить, что массив пустой?**
    - `empty($arr)` или `count($arr) === 0`.
4. **С какого индекса начинаются массивы и можно ли это изменить?**
    - По умолчанию с 0. Изменить можно, явно указав ключ для первого элемента (например, `[1 => 'a', 'b']`), тогда PHP продолжит нумерацию с этого числа.
5. **В чем разница между `count()` и `strlen()`?**
    - `count()` — возвращает количество элементов в массиве.
    - `strlen()` — возвращает количество байт (длину) в строке.
6. **Что произойдет, если обратиться к несуществующему ключу?**
    - PHP выдаст предупреждение (<b>Warning: Undefined array key</b>) и вернет значение null.
7. Как добавить элемент в конец массива, если ты не знаешь его длину?
    - С помощью пустых квадратных скобок: `$arr[] = $value;`. PHP автоматически найдет максимальный числовой ключ и прибавит к нему 1.
8. Чем `print_r()` отличается от `var_dump()`?
    - `print_r` — выводит структуру в легко читаемом виде (ключ => значение).
    - `var_dump` — выводит максимально полную информацию: типы данных (int, string), длину строк и вложенность. Для отладки типов лучше использовать `var_dump`.
9. Как ведут себя ключи при копировании массива?
    - PHP массивы передаются по значению. Это значит, что если ты напишешь `$b = $a`, создастся полная копия данных. Изменение `$b` никак не повлияет на `$a`.

## 4. Стандарт PSR-12 (Массивы)
- Короткий синтаксис `[]` вместо `array()`.
- После запятой всегда ставится пробел: `[1, 2, 3]`.
- В многомерных массивах каждый элемент на новой строке с отступом в 4 пробела.
- Последняя запятая (trailing comma) в многорядных массивах — хороший тон.