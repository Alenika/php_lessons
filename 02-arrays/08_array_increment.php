<?php
	declare(strict_types=1);
	
	/**
	 * Инкремент и декремент элементов
	 * Изменение числовых значений в массиве
	 * ⊗ppPmArEI | 62 of 447
	 */

	// ⊗ppPmArEI №1

	$arr = [
		'a' => 1,
		'b' => 2,
		'c' => 3,
		];
	
	$arr['a']++;
	$arr['a']++;
	$arr['b']--;
	$arr['c']--;
	$arr['c']--;
	
	echo "Результат работы с инкрементом/декрементом:<pre>";
	var_dump($arr);
	echo "</pre>";
	
	/**
	 * ПРАКТИКА
	 */
	
	// Задача: Создай массив $counters = [10, 20].
	// Увеличь первый элемент на 1 через ++, а второй уменьши на 1 через --

	$counters = [10, 20];
	
	$counters[0]++;
	$counters[1]--;
	
	echo "Обновленные счетчики: " . $counters[0] . " и " . $counters[1];
	
	// Счетчик посещений: $stats['views']++ каждый раз, когда кто-то открывает страницу
	
	// Корзина покупок: $cart['items_count']++ при добавлении товара
	
	// Игровые очки: $player['score']++