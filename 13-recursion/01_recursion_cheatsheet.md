# 🌀 Рекурсия в PHP: шпаргалка

## 📌 ОСНОВНАЯ ИДЕЯ
**Рекурсия** — это метод программирования, когда функция вызывает **саму себя** для решения задачи, разбивая ее на меньшие, но идентичные подзадачи.

> **🎪 АНАЛОГИЯ: МАТРЕШКА**
> 1. Открываешь большую матрёшку.
> 2. Внутри — такая же, но меньше.
> 3. Повторяешь, пока не найдешь самую маленькую (**Базовый случай**).
> 4. Возвращаешься назад, закрывая все матрёшки по очереди (**Схлопывание стека**).

---

## 🛠 ДВА ОБЯЗАТЕЛЬНЫХ УСЛОВИЯ
Любая рекурсивная функция обязана иметь:
1. **Базовый случай (Base Case)** — «стоп-кран», условие, при котором рекурсия прекращается. **Без него — бесконечная рекурсия!**
2. **Рекурсивный шаг (Recursive Step)** — вызов функции с измененными (упрощенными) данными.

---

## ⚖️ ПЛЮСЫ И МИНУСЫ

| ✅ Плюсы (Читаемость) | ❌ Минусы (Ресурсы) |
| :--- | :--- |
| **Элегантность:** Код чище и короче для сложных структур. | **Память:** Каждый вызов занимает место в стеке (LIFO). |
| **Естественность:** Идеально для деревьев (DOM, папки, категории). | **Скорость:** Вызов функции дороже, чем обычная итерация. |
| **Упрощение:** Позволяет легко решать задачи типа "Разделяй и властвуй". | **Риск:** Легко получить переполнение стека (Stack Overflow). |

---

## 🎯  КОГДА ИСПОЛЬЗОВАТЬ?

### ✅ Да:
* Обход древовидных структур (меню, файловая система).
* Вложенные данные (парсинг JSON/XML любой глубины).
* Задачи на перебор и комбинаторику.

### ❌ Нет:
* Простые циклы и переборы массивов.
* Когда критически важна скорость и экономия памяти.
* При риске огромной вложенности (больше 1000 уровней).

---

## 🆚 РЕКУРСИЯ vs ИТЕРАЦИЯ

| Критерий | Рекурсия                                | Итерация (Цикл) |
| :--- |:----------------------------------------|:----------------|
| **Память** | Использует стек (O(n))                  | Экономно (O(1)) |
| **Скорость** | Медленнее (накладные расходы на вызовы) | Быстрее         |
| **Отладка** | Сложнее (нужно следить за стеком)       | Проще           |



---

## 💡 Блиц-ответы для интервью
* **Можно ли заменить любую рекурсию циклом?** Да, теоретически это всегда возможно с использованием явного стека (массива).
* **Что такое хвостовая рекурсия?** Это когда вызов себя — последнее действие. В PHP она не оптимизирована, поэтому память всё равно тратится.
* **Как избежать ошибки Memory Limit?** Всегда проверять достижимость базового случая и ограничивать глубину.