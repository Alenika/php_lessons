# 🛠️ Работа с SQL в PhpMyAdmin

---

### 1. Выборка всех записей
**Вопрос:** С помощью какой кнопки можно создать шаблон SQL запроса для выборки всех записей из таблицы?  
**Ответ:** Для этого используется кнопка **`SELECT *`**. Она генерирует базовый запрос:
```sql
SELECT * FROM `users` WHERE 1
```

### 2. Вставка новой записи
**Вопрос:** С помощью какой кнопки можно создать шаблон SQL запроса для вставки записи в таблицу?  
**Ответ:** Используется кнопка **`INSERT`**. Она подготавливает структуру запроса с перечислением всех полей таблицы:
```sql
INSERT INTO `users` (`id`, `name`, `age`, `birthday`) VALUES ([value-1],[value-2],[value-3],[value-4])
```

### 3. Удаление записи
**Вопрос:** С помощью какой кнопки можно создать шаблон SQL запроса для удаления записи из таблицы?
**Ответ:** Используется кнопка **`DELETE`**. Она создает шаблон:
```sql
DELETE FROM `users` WHERE 0
```

### 4. Форматирование кода
**Вопрос:** С помощью какой кнопки можно отформатировать SQL запрос?
**Ответ:** Кнопка **`FORMAT`**. Она выравнивает код, делает его читабельным и расставляет отступы.

### 5. Очистка поля ввода
**Вопрос:** С помощью какой кнопки можно очистить поле для ввода SQL запроса?
**Ответ:** Кнопка **`CLEAR`**. Она мгновенно удаляет весь текст из окна редактора запросов.

