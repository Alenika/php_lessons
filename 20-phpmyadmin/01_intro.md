# 🗄️ PHPMyAdmin

---

## 🎯 ЧТО ТАКОЕ PHPMyAdmin?

**PHPMyAdmin (PMA)** — это веб-приложение с открытым исходным кодом, написанное на PHP, которое предоставляет графический интерфейс для управления базами данных MySQL/MariaDB.

### 📊 Для чего используется:
- **Создание и удаление** баз данных и таблиц
- **Добавление, изменение, удаление** данных
- **Выполнение SQL-запросов**
- **Экспорт и импорт** данных
- **Управление пользователями** и правами доступа
- **Оптимизация** и восстановление таблиц

---

## 📚 ОСНОВНЫЕ ПОНЯТИЯ

### 1. **БАЗА ДАННЫХ (Database)**
    Контейнер, в котором хранятся связанные таблицы. На сервере может быть много разных БД (например, для блога, для магазина).
### 2. **ТАБЛИЦА (Table)**
    Структура внутри БД, похожая на Excel-таблицу, где хранятся данные одного типа (например, таблица users или orders).
### 3. **СТОЛБЕЦ/ПОЛЕ (Column/Field)**
    Определяет тип данных. Например, столбец `email` может хранить только строки, а `age` — только числа.
### 4. **ЗАПИСЬ/СТРОКА (Row/Record)**
    Один конкретный объект в таблице. Например, строка с данными пользователя "Иван".
### 5. **ПЕРВИЧНЫЙ КЛЮЧ (Primary Key)**
    Уникальный идентификатор записи (обычно это `id`). Гарантирует, что в таблице нет двух абсолютно одинаковых строк.
### 6. **ВНЕШНИЙ КЛЮЧ (Foreign Key)**
    Поле, которое связывает одну таблицу с другой. Например, в таблице posts может быть поле `user_id`, которое указывает, какой юзер написал пост.


